# 自动化监控告警平台

**基于K8s部署 Promethus \+ Alertmanage \+ Grafana**

# 创建配置文件

## Promethus\.yml 配置

```YAML
global:
  scrape_interval: 15s          # 采集频率
  evaluation_interval: 15s      # 告警规则评估频率
  external_labels:
    env: 'production'
    region: 'cn-east'

# 引入告警规则文件
rule_files:
  - /etc/prometheus/rules/*.yml

# Alertmanager 地址
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

scrape_configs:

  - job_name: 'blackbox-ruoyi'
    metrics_path: /probe
    params:
      module: [http_2xx]  # 默认模块
    static_configs:
      - targets:
        - http://blackbox-exporter.prom.svc.cluster.local:9115/manage/prometheus
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - target_label: __address__
        replacement: 'blackbox-exporter.monitoring.svc.cluster.local:9115'   # 你的Blackbox Exporter地址

  # ── Prometheus 自监控 ──
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # ── MySQL ──
  - job_name: 'mysql-exporter'
    static_configs:
      - targets: ['192.168.255.142:31904']

  # ── Redis ──
  - job_name: 'redis-exporter'
    static_configs:
      - targets: ['192.168.255.142:31921']

  # k8s
  - job_name: 'kube-state-metrics'
    static_configs:
    - targets:
      - 'kube-state-metrics-svc.monitoring.svc.cluster.local:8080'
      labels:
        env: 'k8s'
        cluster: 'ruoyi-k8s'
  # k8s-node
  - job_name: 'node-exporter'
    static_configs:
    - targets:
      - '192.168.255.140:9100'
      labels:
        node: 'ci'      
        env: 'jenkins'
    - targets:
      - '192.168.255.141:9100'
      labels:
        node: 'k8s-master'        
        env: 'k8s'
        cluster: 'ruoyi-k8s'
    - targets:
      - '192.168.255.142:9100'
      labels:
        node: 'k8s-worker'          # 对应 Grafana 变量里选的值
        env: 'k8s'
        cluster: 'ruoyi-k8s'
```

## rules/node\-alters\.yml 规则配置

```YAML
groups:
  - name: node_alerts
    rules:

      # ① 实例宕机
      - alert: InstanceDown
        expr: up == 0
        for: 1m          # 持续 1 分钟才触发，避免抖动
        labels:
          severity: critical
        annotations:
          summary: "实例 {{ $labels.instance }} 宕机"
          description: "{{ $labels.job }} / {{ $labels.instance }} 已离线超过 1 分钟"

      # ② CPU 使用率 > 80%
      - alert: HighCPUUsage
        expr: 100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 50
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "CPU 告警 {{ $labels.instance }}"
          description: "CPU 使用率 {{ printf \"%.1f\" $value }}%，超过 50% 已持续 1 分钟"

      # ③ 内存可用率 < 10%
      - alert: LowMemory
        expr: (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes) * 100 < 30
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "内存不足 {{ $labels.instance }}"
          description: "可用内存 {{ printf \"%.1f\" $value }}%，低于 30%"

      # ④ 磁盘剩余空间 < 15%
      - alert: DiskSpaceLow
        expr: (node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"}) * 100 < 35
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "磁盘空间告警 {{ $labels.instance }}"
          description: "/ 分区剩余 {{ printf \"%.1f\" $value }}%"
```

## rules/k8s\-alerts\.yml k8s集群规则

```YAML
groups:
  - name: kube-state-metrics.rules
    rules:
      # ========== Pod 异常 ==========
      - alert: PodNotRunning
        expr: kube_pod_status_phase{phase!~"Running|Succeeded"} == 1
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Pod 非正常状态"
          description: "{{ $labels.namespace }}/{{ $labels.pod }} 状态为 {{ $labels.phase }}，持续超过2分钟"
      - alert: PodCrashLooping
        expr: rate(kube_pod_container_status_restarts_total[5m]) * 300 > 1
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Pod 频繁重启"
          description: "{{ $labels.namespace }}/{{ $labels.pod }} 容器 {{ $labels.container }} 5分钟内重启超过1次"
      - alert: PodNotReady
        expr: kube_pod_status_ready{condition="true"} == 0
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Pod 未就绪"
          description: "{{ $labels.namespace }}/{{ $labels.pod }} Ready状态为false，持续超过2分钟"
      # ========== Deployment 异常 ==========
      - alert: DeploymentReplicasMismatch
        expr: |
          kube_deployment_spec_replicas
            !=
          kube_deployment_status_replicas_available
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Deployment 可用副本数不足"
          description: "{{ $labels.namespace }}/{{ $labels.deployment }} 期望副本数与可用副本数不符，持续超过5分钟"
      - alert: DeploymentUnavailable
        expr: kube_deployment_status_replicas_available == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Deployment 完全不可用"
          description: "{{ $labels.namespace }}/{{ $labels.deployment }} 可用副本数为0"
      # ========== Node 异常 ==========
      - alert: NodeNotReady
        expr: kube_node_status_condition{condition="Ready",status="true"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Node 未就绪"
          description: "节点 {{ $labels.node }} Ready条件为false，持续超过1分钟"
      - alert: NodeMemoryPressure
        expr: kube_node_status_condition{condition="MemoryPressure",status="true"} == 1
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Node 内存压力"
          description: "节点 {{ $labels.node }} 存在内存压力"
      - alert: NodeDiskPressure
        expr: kube_node_status_condition{condition="DiskPressure",status="true"} == 1
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Node 磁盘压力"
          description: "节点 {{ $labels.node }} 存在磁盘压力"
      # ========== StatefulSet 异常 ==========
      - alert: StatefulSetReplicasMismatch
        expr: |
          kube_statefulset_status_replicas_ready
            !=
          kube_statefulset_status_replicas
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "StatefulSet 副本数不足"
          description: "{{ $labels.namespace }}/{{ $labels.statefulset }} 就绪副本数与期望不符，持续超过5分钟"
      # ========== PVC 异常 ==========
      - alert: PVCNotBound
        expr: kube_persistentvolumeclaim_status_phase{phase!="Bound"} == 1
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "PVC 未绑定"
          description: "{{ $labels.namespace }}/{{ $labels.persistentvolumeclaim }} 状态为 {{ $labels.phase }}"
```

## rules/mysql\-alerts\.yml k8s集群规则

```YAML
groups:
- name: mysql_rules      # 新增
  rules:
    - alert: MySQLDown
      expr: mysql_up == 0
      for: 30s
      labels:
        severity: critical
      annotations:
        summary: "MySQL 宕机"
        description: "{{ $labels.instance }} MySQL 无法连接"
    
    - alert: MySQLSlaveNotRunning
      expr: mysql_slave_status_slave_sql_running == 0
      for: 30s
      labels:
        severity: critical
      annotations:
        summary: "MySQL 主从同步异常"
        description: "{{ $labels.instance }} SQL线程停止"
    
    - alert: MySQLSlaveDelay
      expr: mysql_slave_status_seconds_behind_master > 30
      for: 1m
      labels:
        severity: warning
      annotations:
        summary: "MySQL 主从延迟过大"
        description: "{{ $labels.instance }} 延迟 {{ $value }} 秒"
```

## ruler/redis\-alerts\.yml

```YAML
groups:
- name: redis_rules      # 新增
  rules:
    - alert: RedisDown
      expr: redis_up == 0
      for: 30s
      labels:
        severity: critical
      annotations:
        summary: "Redis 宕机"
        description: "{{ $labels.instance }} Redis 无法连接"
    - alert: RedisMemoryHigh
      expr: redis_memory_used_bytes / redis_memory_max_bytes * 100 > 80
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Redis 内存使用率过高"
        description: "{{ $labels.instance }} 内存使用率 {{ $value }}%"
    - alert: RedisConnections
      expr: redis_connected_clients > 100
      for: 5m
      labels:
        severity: warning
      annotations:
        summary: "Redis 连接数过多"
        description: "{{ $labels.instance }} 当前连接数 {{ $value }}"
```

## alertmanager\.yml 告警配置

```YAML
global:
  resolve_timeout: 5m        # 告警恢复后等待多久发送 resolved 消息

route:
  group_by: ['alertname', 'instance']   # 同一实例的告警合并发一条
  group_wait: 30s            # 同一组第一条告警等待 30s，方便合并
  group_interval: 5m         # 同一组后续告警间隔
  repeat_interval: 1h        # 告警未恢复时重复通知间隔
  receiver: 'dingtalk'       # 默认接收者

  routes:
    # critical 级别单独路由
    - match:
        severity: critical
      receiver: 'dingtalk'
      repeat_interval: 30m   # 严重告警 30 分钟重复一次

receivers:
  # 钉钉（走 adapter）
  - name: 'dingtalk'
    webhook_configs:
      - url: 'http://dingtalk-webhook:8060/dingtalk/webhook1/send'
        send_resolved: true  # 告警恢复时也发通知
inhibit_rules:
  # critical 触发时抑制同实例的 warning，防止刷屏
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['instance']
```

## dingtalk/config\.yml 钉钉告警

```YAML
targets:
  webhook1: #创建机器人时生成
    url: https://oapi.dingtalk.com/robot/send?access_token=523e4a28c25501342a331a698ff63cc7cceaa910bf6a98008009c8100ced7938
    secret: SECfce4c0bcbdf02ef6679ff997d1437dd6c949d021b713165792721e20ba1fb930
    message:
      title: '{{ template "ding.link.title" . }}'
      text: '{{ template "ding.link.content" . }}'
```

# 使用K8s安装服务

使用k8s安装服务

```YAML
# ============================================================
# Namespace
# ============================================================
apiVersion: v1
kind: NameSpace
metadata:
  name: prom
---
# ============================================================
# PersistentVolumeClaim - Prometheus
# ============================================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data
  namespace: prom
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: nfs-client   # 替换为你的 StorageClass

---
# ============================================================
# PersistentVolumeClaim - Grafana
# ============================================================
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: prom
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: nfs-client   # 替换为你的 StorageClass

---
# ============================================================
# Deployment - Prometheus
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: prom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus:v2.51.0
          args:
            - '--config.file=/etc/prometheus/prometheus.yml'
            - '--storage.tsdb.path=/prometheus'
            - '--storage.tsdb.retention.time=30d'
            - '--web.enable-lifecycle'
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus/prometheus.yml
              subPath: prometheus.yml
            - name: rules
              mountPath: /etc/prometheus/rules
            - name: data
              mountPath: /prometheus
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 2Gi
      volumes:
        - name: config
          configMap:
            name: prometheus-config
        - name: rules
          configMap:
            name: prometheus-rules   # 需要单独创建，见下方说明
        - name: data
          persistentVolumeClaim:
            claimName: prometheus-data

---
# ============================================================
# Service - Prometheus
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: prom
spec:
  type: NodePort
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30090

---
# ============================================================
# Deployment - Grafana
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: prom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:10.4.0
          ports:
            - containerPort: 3000
          env:
            - name: GF_SECURITY_ADMIN_USER
              value: "admin"
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: "Admin@2024"
            - name: GF_USERS_ALLOW_SIGN_UP
              value: "false"
            - name: GF_SERVER_ROOT_URL
              value: "http://192.168.255.100:30030"
          volumeMounts:
            - name: data
              mountPath: /var/lib/grafana
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: grafana-data

---
# ============================================================
# Service - Grafana
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: prom
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030

---
# ============================================================
# Deployment - Alertmanager
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alertmanager
  namespace: prom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alertmanager
  template:
    metadata:
      labels:
        app: alertmanager
    spec:
      containers:
        - name: alertmanager
          image: prom/alertmanager:v0.27.0
          ports:
            - containerPort: 9093
          volumeMounts:
            - name: config
              mountPath: /etc/alertmanager/alertmanager.yml
              subPath: alertmanager.yml
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi
      volumes:
        - name: config
          configMap:
            name: alertmanager-config

---
# ============================================================
# Service - Alertmanager
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: prom
spec:
  type: NodePort
  selector:
    app: alertmanager
  ports:
    - port: 9093
      targetPort: 9093
      nodePort: 30093

---
# ============================================================
# Deployment - Blackbox Exporter
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blackbox-exporter
  namespace: prom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blackbox-exporter
  template:
    metadata:
      labels:
        app: blackbox-exporter
    spec:
      containers:
        - name: blackbox-exporter
          image: prom/blackbox-exporter:latest
          ports:
            - containerPort: 9115
          volumeMounts:
            - name: config
              mountPath: /etc/blackbox_exporter/config.yml
              subPath: config.yml
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
      volumes:
        - name: config
          configMap:
            name: blackbox-config

---
# ============================================================
# Service - Blackbox Exporter
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: blackbox-exporter
  namespace: prom
spec:
  selector:
    app: blackbox-exporter
  ports:
    - port: 9115
      targetPort: 9115

---
# ============================================================
# Deployment - DingTalk Webhook
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dingtalk-webhook
  namespace: prom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dingtalk-webhook
  template:
    metadata:
      labels:
        app: dingtalk-webhook
    spec:
      containers:
        - name: dingtalk-webhook
          image: timonwong/prometheus-webhook-dingtalk:v2.1.0
          args:
            - '--config.file=/config/config.yml'
          ports:
            - containerPort: 8060
          volumeMounts:
            - name: config
              mountPath: /config/config.yml
              subPath: config.yml
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
      volumes:
        - name: config
          configMap:
            name: dingtalk-config

---
# ============================================================
# Service - DingTalk Webhook
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: dingtalk-webhook
  namespace: prom
spec:
  selector:
    app: dingtalk-webhook
  ports:
    - port: 8060
      targetPort: 8060
```

# 部署监控节点

重新打包后端项目 \-\-\> 配置 `promethues.yml` \-\-\> 热重启

## blackbox\-exporter\.yml

```YAML
# ============================================================
# Deployment - Blackbox Exporter
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blackbox-exporter
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blackbox-exporter
  template:
    metadata:
      labels:
        app: blackbox-exporter
    spec:
      containers:
        - name: blackbox-exporter
          image: 192.168.255.140:80/pub/blackbox:latest
          ports:
            - containerPort: 9115
          volumeMounts:
            - name: config
              mountPath: /etc/blackbox_exporter/config.yml
              subPath: config.yml
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
      volumes:
        - name: config
          configMap:
            name: blackbox-config

---
# ============================================================
# Service - Blackbox Exporter
# ============================================================
apiVersion: v1
kind: Service
metadata:
  name: blackbox-exporter
  namespace: monitoring
spec:
  selector:
    app: blackbox-exporter
  ports:
    - port: 9115
      targetPort: 9115
```

## kube\-state\-metrics\.yml

```YAML
# kube-state-metrics.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kube-state-metrics
  namespace: monitoring
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kube-state-metrics
rules:
- apiGroups: [""]
  resources:
  - nodes
  - pods
  - services
  - endpoints
  - persistentvolumeclaims
  - persistentvolumes
  - namespaces
  - replicationcontrollers
  verbs: ["list", "watch"]
- apiGroups: ["apps"]
  resources:
  - daemonsets
  - deployments
  - replicasets
  - statefulsets
  verbs: ["list", "watch"]
- apiGroups: ["batch"]
  resources:
  - cronjobs
  - jobs
  verbs: ["list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kube-state-metrics
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kube-state-metrics
subjects:
- kind: ServiceAccount
  name: kube-state-metrics
  namespace: monitoring
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-state-metrics
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-state-metrics
  template:
    metadata:
      labels:
        app: kube-state-metrics
    spec:
      serviceAccountName: kube-state-metrics
      containers:
      - name: kube-state-metrics
        image: registry.k8s.io/kube-state-metrics/kube-state-metrics:v2.12.0
        ports:
        - containerPort: 8080
          name: metrics
        - containerPort: 8081
          name: telemetry
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 300m
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics-svc
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: kube-state-metrics
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
    nodePort: 31116
```

## mysql\-exporter\.yml

```YAML
# 1. 创建 Secret 存储连接信息
apiVersion: v1
kind: Secret
metadata:
  name: mysql-exporter-config
  namespace: monitoring   # 改成你实际的 namespace
type: Opaque
stringData:
  .my.cnf: |
    [client]
    host=ruoyi-mysql.ruoyi-cloud.svc.cluster.local
    port=3306
    user=exporter
    password=password
---
# mysql-exporter.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-exporter
  namespace: monitoring
  labels:
    app: mysql-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-exporter
  template:
    metadata:
      labels:
        app: mysql-exporter
    spec:
      containers:
      - name: mysql-exporter
        image: prom/mysqld-exporter:v0.15.1
        args:
        - "--config.my-cnf=/etc/mysql/.my.cnf"
        - "--mysqld.address=ruoyi-mysql.ruoyi-cloud.svc.cluster.local:3306"
        volumeMounts:
        - name: mysql-config
          mountPath: /etc/mysql
          readOnly: true
        ports:
        - containerPort: 9104
          name: metrics
      volumes:
      - name: mysql-config
        secret:
          secretName: mysql-exporter-config
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-exporter-svc
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: mysql-exporter
  ports:
  - name: metrics
    port: 9104
    targetPort: 9104
    nodePort: 31904     # 固定端口，外部 Prometheus 用这个
```

## node\-exporter\.yml

```YAML
# node-exporter-daemonset.yaml
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: node-exporter
  namespace: monitoring
  labels:
    app: node-exporter
spec:
  selector:
    matchLabels:
      app: node-exporter
  template:
    metadata:
      labels:
        app: node-exporter
    spec:
      hostNetwork: true       # 使用宿主机网络，能采集真实网络指标
      hostPID: true           # 能看到宿主机所有进程
      tolerations:
      - key: node-role.kubernetes.io/control-plane   # master 节点也部署
        operator: Exists
        effect: NoSchedule
      containers:
      - name: node-exporter
        image: prom/node-exporter:v1.8.1
        args:
        - "--path.procfs=/host/proc"
        - "--path.sysfs=/host/sys"
        - "--path.rootfs=/host/root"
        - "--collector.filesystem.mount-points-exclude=^/(dev|proc|sys|var/lib/docker/.+)($|/)"
        ports:
        - containerPort: 9100
          name: metrics
        volumeMounts:
        - name: proc
          mountPath: /host/proc
          readOnly: true
        - name: sys
          mountPath: /host/sys
          readOnly: true
        - name: root
          mountPath: /host/root
          readOnly: true
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 200m
            memory: 128Mi
      volumes:
      - name: proc
        hostPath:
          path: /proc
      - name: sys
        hostPath:
          path: /sys
      - name: root
        hostPath:
          path: /
```

## redis\-exporter\.yml

```YAML
# redis-exporter.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-exporter
  namespace: monitoring
  labels:
    app: redis-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-exporter
  template:
    metadata:
      labels:
        app: redis-exporter
    spec:
      containers:
      - name: redis-exporter
        image: oliver006/redis_exporter:v1.62.0
        env:
        - name: REDIS_ADDR
          value: "redis://ruoyi-redis.ruoyi-cloud.svc.cluster.local:6379"   # 换成你的 Redis Service 名
        # 如果 Redis 有密码，加下面这行
        # - name: REDIS_PASSWORD
        #   valueFrom:
        #     secretKeyRef:
        #       name: redis-secret
        #       key: REDIS_PASSWORD
        ports:
        - containerPort: 9121
          name: metrics
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 200m
            memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: redis-exporter-svc
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: redis-exporter
  ports:
  - name: metrics
    port: 9121
    targetPort: 9121
    nodePort: 31921     # 固定端口
```

## 更新\&测试

```Bash
#!/bin/bash
PROM_DIR="."   # 替换为你的实际路径
NS="prom"

kubectl create configmap prometheus-config \
  --from-file=prometheus.yml=$PROM_DIR/prometheus.yml \
  -n $NS --dry-run=client -o yaml | kubectl apply -f -

kubectl create configmap prometheus-rules \
  --from-file=$PROM_DIR/rules/ \
  -n $NS --dry-run=client -o yaml | kubectl apply -f -

kubectl create configmap alertmanager-config \
  --from-file=alertmanager.yml=$PROM_DIR/alertmanager.yml \
  -n $NS --dry-run=client -o yaml | kubectl apply -f -

kubectl create configmap blackbox-config \
  --from-file=config.yml=$PROM_DIR/blackbox.yml \
  -n $NS --dry-run=client -o yaml | kubectl apply -f -

kubectl create configmap dingtalk-config \
  --from-file=config.yml=$PROM_DIR/dingtalk/config.yml \
  -n $NS --dry-run=client -o yaml | kubectl apply -f -

kubectl delete -f prom.yml

kubectl apply -f prom.yml
echo "All ConfigMaps created."
```

```Bash
热重载
curl -X POST http://localhost:9090/-/reload
查看状态
http://localhost:9090/targets
```

重新打包后端项目 \-\-\> 配置 `promethues.yml` \-\-\> 热重启

# 告警测试

## 手动触发告警

```Bash
#手动触发告警
curl -X POST http://localhost:9093/api/v2/alerts \   -H 'Content-Type: application/json' \   -d '[{     "labels": {"alertname":"TestAlert","instance":"test-server","severity":"warning"},     "annotations": {"summary":"测试告警，验证钉钉通道"},     "startsAt": "2024-01-01T00:00:00Z"   }]'
```

## 压力测试告警

```Bash
#CPU压力
stress --cpu 4 --timeout 180
sleep 61
#内存压力
python3 -c "x = bytearray(2000 * 1024 * 1024); import time; time.sleep(60)"
```

# Grafana监控面板

- kube\-state\-metrics\-v2（修正版）

- MySQL Overview

- Node Exporter Full

- Prometheus Blackbox Exporter



> （注：部分内容可能由 AI 生成）
